<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Blog App</title>
</head>
<body>
    <div class="container">
        <h1>My Blog</h1>
        <form id ="form" action="/create-post" method="POST">
            <input type="text" name="postTitle" placeholder="Post Title" required>
            <textarea name="postContent" placeholder="Write a post..." required></textarea>
            <button type="submit">Post</button>
        </form>
        <div>
            <% if (posts.length > 0) { %>
                <% posts.forEach((post, index) => { %>
                    <div class="post">
                        <h2><%= post.title %></h2>
                        <p><%- post.content.replace(/\n/g, '<br>') %></p>
                        <form action="/delete-post" method="POST" style="display: inline;">
                            <input type="hidden" name="index" value="<%= index %>">
                            <button id ="smallbuttons" type="submit">Delete</button>
                        </form>
                        <form action="/edit-post" method="GET" style="display: inline;">
                            <input type="hidden" name="index" value="<%= index %>">
                            <input type="hidden" name="title" value="<%= post.title %>">
                            <input type="hidden" name="content" value="<%= post.content %>">
                            <button id ="smallbuttons" type="submit">Edit</button>
                        </form>
                    </div>
                <% }); %>
            <% } else { %>
                <p>No posts yet! Be the first to add one.</p>
            <% } %>
        </div>
        <footer>
            <p>Check out my GitHub: 
                <a href="https://github.com/dhanushsriram" target="_blank">GitHub Profile</a>
            </p>
        </footer>
    </div>
</body>

</html>
